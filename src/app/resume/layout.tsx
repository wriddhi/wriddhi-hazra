export const metadata = {
  title: "Resume | Wriddhi Hazra",
  description: "Looking to hire? Check out my resume.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
