export const metadata = {
  title: "Contact | Wriddhi Hazra",
  description: "Interested in working together?",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
